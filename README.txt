AUCTION ELEVEN — CINEMATIC UI v4 PATCH
======================================

This patch is for a project that already has the previous 30-manager-crest/server badge patch applied.
It replaces only frontend files/assets and does NOT include your footballer image pack.

Main fixes/upgrades:
- Brand corrected to AUCTION ELEVEN.
- Home screen rebuilt into manager-creation + cinematic football hero layout.
- Keeps all 30 custom manager crests.
- Normal Auction gets stronger broadcast/stadium hierarchy and larger bid presentation.
- Blind Auction rebuilt into a 3-column quiz layout: player reveal, quiz/clues, rival manager list.
- Blind player photos now use a dark cinematic cover backdrop + a full uncropped foreground image.
- Random directional wipe has a visible gold reveal edge and progress bar.
- Real WAV sound effects added for UI clicks, bids, timer ticks, correct/wrong guesses, sold, round transitions and ready confirmation.
- SFX button now clearly shows SFX ON/OFF and plays an audible confirmation when enabled.
- Stadium/pitch background assets added.
- Lobby/results receive matching sports-broadcast styling.

Copy these folders/files into the ROOT of your existing project and allow Windows to merge/replace them.
Do not delete apps/server/player-images.
