AUCTION ELEVEN v2.3 — BLIND AUCTION

1. Extract this ZIP into:
   C:\Auction\auction-eleven

2. Allow Windows to replace files in the destination.

3. Do not delete .env files.

4. Open PowerShell in C:\Auction\auction-eleven and run:

   npm install
   npm run typecheck
   npm test
   npm run build

   These are separate commands. Do NOT write them as one npm command.

5. Or run the included verifier:

   Set-ExecutionPolicy -Scope Process Bypass
   .\VERIFY-v2.3.ps1

6. Do not push/deploy if any validation step fails.

7. If all pass:

   git add packages/shared/src/index.ts apps/server/src/gameEngine.ts apps/server/src/guessing.ts apps/server/src/blindReveal.ts apps/server/src/roomManager.ts apps/server/src/index.ts apps/server/test/guessing.test.ts apps/server/test/roomFlow.test.ts apps/web/src/main.tsx apps/web/src/styles.css

   git commit -m "Add secure Blind Auction guess mode"

   git push origin main

Both Render and Vercel should redeploy because shared, backend and frontend code changed.
